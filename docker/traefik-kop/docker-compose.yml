services:
  traefik-kop:
    image: "ghcr.io/jittering/traefik-kop:0.20.1"
    restart: unless-stopped
    volumes:
      - /run/user/1000/docker.sock:/var/run/docker.sock
    environment:
      - "REDIS_ADDR=${REDIS_ADDRESS}"
      - "BIND_IP=${BIND_IP}"