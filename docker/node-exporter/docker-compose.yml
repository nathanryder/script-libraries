services:
  node-exporter:
    hostname: ${HOSTNAME}
    image: prom/node-exporter:v1
    container_name: node-exporter
    pull_policy: always
    restart: unless-stopped
    ports:
      - "9100:9100"
    command:
      - "--path.rootfs=/host"
    volumes:
      - /:/host:ro,rslave